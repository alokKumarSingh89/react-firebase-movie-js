import {
    makeStyles,
    createStyles
} from "@material-ui/core/styles";


export default makeStyles((theme) =>
    createStyles({
        root: {
           backgroundColor:"#f2f2f2",
            padding: 100
        }
    })
);
